# Trello API 認証情報。.env は .gitignore 済み。
# monolith-gas/trello-tools/trello-card-automator/.env と同じ値を使う。
#
# 値を複製したくない場合は .env を作らず、環境変数で渡すか
# TRELLO_ENV_FILE で既存の .env を直接指す。
#   export TRELLO_ENV_FILE=/path/to/trello-card-automator/.env
TRELLO_API_KEY=
TRELLO_TOKEN=
